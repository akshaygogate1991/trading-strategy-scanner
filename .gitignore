__pycache__/
*.py[cod]
.venv/
venv/
.env
.streamlit/secrets.toml
logs/

# Office documents and their lock files. A 237 KB transcript and a Word lock
# file ("~$...") both reached the public repo because Word had the document
# open, which made the delete fail silently.
*.docx
*.doc
*.xlsx
~$*

# research/backtest scratch output — cached market data and computed results.
# These regenerate from Angel One on demand, so they do not belong in the repo:
# 690 KB of CSVs slows every Streamlit Cloud deploy for no benefit.
data/
sma_data/
ema_elliott_mask.json
*_events.csv
*_trades.csv
*_analysis.csv
